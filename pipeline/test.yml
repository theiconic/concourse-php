---
platform: linux

image_resource:
  type: docker-image
  source: {repository: composer/composer, tag: alpine}

inputs:
- name: built-concourse-php

outputs:
- name: test-results
  path: built-concourse-php/test-results

run:
  dir: built-concourse-php
  path: pipeline/test.sh
